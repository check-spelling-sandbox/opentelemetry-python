name: Core Contrib Test

on:
  push:
    branches-ignore:
    - 'release/*'
  pull_request:

jobs:
  contrib_0:
    uses: check-spelling-sandbox/opentelemetry-python-contrib/.github/workflows/core_contrib_test_0.yml@fork-friendly
    with:
      CORE_REPO: ${{ github.repository }}
      CORE_REPO_SHA: ${{ github.sha }}
      CONTRIB_REPO_SHA: main
